// Optional public configuration for GitHub Pages.
// Set this to the connection repo's published connection.json URL before publishing,
// or pass ?manifest=https://.../connection.json in the page URL.
window.BLACKJACK_CONNECTION_MANIFEST_URL = "";
